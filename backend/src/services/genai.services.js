export const summarizeText = async (text) => {
  // Dummy function for summarizing text
  return new Promise((resolve) => {
    setTimeout(() => {
      resolve("This is a dummy summary.");
    }, 1000);
  });
};
